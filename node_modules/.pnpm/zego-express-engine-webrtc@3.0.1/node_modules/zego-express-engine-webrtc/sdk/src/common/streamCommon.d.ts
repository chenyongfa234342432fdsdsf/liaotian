import { StateCenter } from "./stateCenter";
import { ZegoStreamCenter } from "./streamCenter";
import { ZegoExpressWebRTM, ZegoLogger } from "./zego.entity";
export declare class StreamCommon {
    private streamCenter;
    private stateCenter;
    private logger;
    private rtm;
    constructor(streamCenter: ZegoStreamCenter, stateCenter: StateCenter, logger: ZegoLogger, rtm: ZegoExpressWebRTM);
    setStreamExtraInfo(streamID: string, extraInfo: string): Promise<{
        errorCode: number;
        extendedData: string;
    }>;
}
