import { LogCache } from './index';
import { LogStrategy } from './strategy/log-strategy';
export declare class LogSender {
    private ws;
    private cache;
    private strategy;
    constructor(ws: WebSocket, cache: LogCache, strategy: LogStrategy);
    active(): void;
}
