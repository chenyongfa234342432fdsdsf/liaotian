import { LOG_STRATEGY, Logger } from '../../zego.entity';
import { ConfigState } from '../common/config.state';
import { LogCache } from './index';
export declare class NetworkContext {
    private cache;
    private configState;
    private innerLogger;
    private websocket;
    private websocketState;
    private senderBuilder;
    private sender;
    constructor(cache: LogCache, configState: ConfigState, innerLogger: Logger);
    get url(): string;
    set url(v: string);
    get strategy(): LOG_STRATEGY;
    setStrategy(strategy: LOG_STRATEGY): void;
    addRemoteWsLog(log: string): void;
    stopWebSocketServer(): void;
    private updateSender;
    private updateWebSocketState;
    openWebSocketLogServer(url: string): void;
    closed(): boolean;
    connected(): boolean;
    connecting(): boolean;
    wsSend(log: string): void;
    destroy(): void;
}
