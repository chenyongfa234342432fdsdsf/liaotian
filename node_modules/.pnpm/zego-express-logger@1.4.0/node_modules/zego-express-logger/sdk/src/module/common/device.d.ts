export declare class Device {
    private static storageKey;
    private static isEnvSupport;
    static bin2hex(s: string): string;
    static getCanvasFingerprint(domain: string): string;
    static generateUUID(): string;
    static id(): string;
}
