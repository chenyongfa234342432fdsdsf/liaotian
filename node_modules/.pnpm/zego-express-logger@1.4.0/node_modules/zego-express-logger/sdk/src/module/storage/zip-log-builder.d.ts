import { ConfigState } from '../common';
export declare class ZipLogBuilder {
    private configState;
    private logCtx;
    private excludedKeys;
    private buildedCount;
    private sdkid;
    constructor(configState: ConfigState);
    build(log: string): string;
    buildFullMessageLog(log: string): string;
}
