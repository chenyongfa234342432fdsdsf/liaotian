import { LOG_STRATEGY } from '../../zego.entity';
import { LogCache } from './index';
import { LogSender } from './log-sender';
export declare class LogSenderBuilder {
    private ws?;
    private cache?;
    private _strategy;
    get strategy(): LOG_STRATEGY;
    setWs(v: WebSocket): void;
    setCache(v: LogCache): void;
    setStrategy(v: LOG_STRATEGY): void;
    build(): LogSender;
}
